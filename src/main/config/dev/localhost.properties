# Instance name (define war file name)
INSTANCE_NAME=opensilex-phis
# Instance version
INSTANCE_VERSION=4.0
# Maven path to binary
MAVEN_BIN=/home/vincent/netbeans/java/maven/bin/mvn
# Path to JDK without trailing slash
JDK=/usr/lib/jvm/jdk-11.0.1
# Path to tomcat without trailing slash
TOMCAT_PATH=/home/vincent/apache-tomcat-9.0.13
# Debug options
JPDA_OPTS="-agentlib:jdwp=transport=dt_socket,address=localhost:8000,server=y,suspend=n"
# Tomacat Manager base URI without trailing slash (optional)
TOMCAT_MANAGER_BASE_URI=http://127.0.0.1:8080/manager
# Tomcat Manager Account (optional)
TOMCAT_MANAGER_ACCOUNT=tomcat-admin
# Tomcat Manager Password (need to escape ! -> /! , / -> // )
TOMCAT_MANAGER_PASS=azerty
# Leave this line at the end of the file